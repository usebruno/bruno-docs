// Note: Metadata is now handled via Next.js App Router in app/layout.jsx
// This component is kept for backwards compatibility but no longer renders anything
// as all metadata is configured through the root layout's metadata export.

export const Head = () => null