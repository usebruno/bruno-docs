import jsonIndexCache from "@/lib/cache/index-cache/indexCache.json";
import {Typography} from "@/components/ui/typography";
import {Button} from "@/components/ui/button";
import Link from "next/link";
import {Separator} from "@/components/ui/separator";

export default function Index() {
  return (
    <div className="flex flex-col mt-2">
      {jsonIndexCache?.map((item, index) => (
        <div key={`index_folder_${item.name}`} className="flex flex-col">
          <Separator className="my-4" />
          <Typography variant="h2" className="capitalize border-0 mt-2">
            {item.name}
          </Typography>
          <ul className="flex flex-col mt-4 ml-5 w-fit items-start list-disc">
            {item.children?.map((child) => (
              <li key={`indexButton_${child.name}`} className="mt-0.5">
                <Button variant="link" className="px-1 capitalize" asChild>
                  <Link href={`/${child.path}`}>
                    {child.name}
                  </Link>
                </Button>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  );
}

