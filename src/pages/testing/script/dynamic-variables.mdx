import { Callout } from 'nextra/components'

# Dynamic Variables

Dynamic variables enable you to generate and use random data in your testing workflow. Bruno uses the [faker.js](https://fakerjs.dev/) library to generate random data. You can use these variables like any other variables in Bruno. The syntax to use dynamic variables is `{{$randomData}}`, and you can use them in the request body, authentication, parameters, and other fields.

<Callout emoji="">
 Dynamic variables are case-sensitive and follow the camelCase convention. 
</Callout>

## Available Dynamic Variables

The following table provides a comprehensive list of available dynamic variables, their examples, and sample values:

| Category | Variable | Examples |
|----------|---------|---------------|
| Timestamp | `{{$timestamp}}` | `1562757107`, `1562757108`, `1562757109` |
| Numbers | `{{$randomNumber}}` | `42`, `3.14`, `1000` |
| Colors | `{{$randomColor}}` | `#FF0000`, `rgb(255, 0, 0)`, `hsl(0, 100%, 50%)` |
| IP Addresses | `{{$randomIp}}` | `192.168.1.1`, `2001:0db8:85a3:0000:0000:8a2e:0370:7334` |
| Images | `{{$randomImage}}` | `https://picsum.photos/200/300`, `https://i.pravatar.cc/300` |
| Names | `{{$randomName}}` | `John Doe`, `Jane Smith`, `Robert Johnson` |
| Professions | `{{$randomJob}}` | `Software Engineer`, `Product Manager`, `Data Scientist` |
| Locations | `{{$randomLocation}}` | `New York, USA`, `London, UK`, `Tokyo, Japan` |
| Finance | `{{$randomAmount}}` | `$1,234.56`, `€789.00`, `¥10,000` |
| Businesses | `{{$randomCompany}}` | `TechCorp Inc.`, `Global Solutions Ltd.` |
| Databases | `{{$randomDatabase}}` | `users`, `orders`, `products` |
| Domains | `{{$randomDomain}}` | `example.com`, `test.org`, `demo.net` |
| Emails | `{{$randomEmail}}` | `john.doe@example.com`, `jane.smith@test.org` |
| Usernames | `{{$randomUsername}}` | `johndoe123`, `janesmith456` |
| Files | `{{$randomFile}}` | `document.pdf`, `image.jpg`, `/path/to/file.txt` |
| Stores | `{{$randomStore}}` | `Electronics`, `Clothing`, `Home & Garden` |
| Grammar | `{{$randomWord}}` | `quick`, `brown`, `fox` |
| Lorem Ipsum | `{{$randomText}}` | `Lorem ipsum dolor sit amet`, `Consectetur adipiscing elit` |

## Usage Examples

Here are some common use cases for dynamic variables:

1. **Test Data Generation**:
   ```json
   {
     "name": "{{$randomName}}",
     "email": "{{$randomEmail}}",
     "age": "{{$randomNumber}}",
     "timestamp": "{{$timestamp}}"
   }
   ```

2. **Authentication**:
   ```json
   {
     "username": "{{$randomUsername}}",
     "password": "{{$randomPassword}}",
     "lastLogin": "{{$timestamp}}"
   }
   ```

3. **Location-based Testing**:
   ```json
   {
     "address": "{{$randomLocation}}",
     "city": "{{$randomCity}}",
     "country": "{{$randomCountry}}",
     "coordinates": {
       "latitude": "{{$randomLatitude}}",
       "longitude": "{{$randomLongitude}}"
     }
   }
   ```

For a complete list of available methods and their detailed usage, please refer to the [faker.js documentation](https://fakerjs.dev/). 